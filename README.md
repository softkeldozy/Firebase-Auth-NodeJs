This is a simple firebase auth app with nodejs

## Getting Started

First, run the installation for dependencies:

```bash
npm install
```

Also you will be needing a service account secret key from your firebase console
